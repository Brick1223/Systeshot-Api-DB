import { sequelize, Detalle_pedido } from '../config/database.mjs';
import { DataTypes } from 'sequelize';

// Modelo de DetallePedido
const DetallePedido = Detalle_pedido;

export default DetallePedido;

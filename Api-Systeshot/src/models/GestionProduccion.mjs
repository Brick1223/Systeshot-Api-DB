import { sequelize, GestionProduccion } from '../config/database.mjs';
import { DataTypes } from 'sequelize';

// Modelo de GestionProduccion
const GestionProduccion = GestionProduccion;

export default GestionProduccion;
